version: "2"
sql:
- schema: "pkg/maindb/migrations"
  queries: "queries"
  engine: "postgresql"
  gen:
    go: 
      package: "maindb"
      out: "pkg/maindb"
      emit_result_struct_pointers: true
      emit_interface: true